name: Security Audit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Monday at 06:00 UTC
    - cron: '0 6 * * 1'

jobs:
  frontend:
    name: Frontend (npm audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cd frontend && npm ci
      - run: cd frontend && npm audit --audit-level=high

  mcp-server:
    name: MCP Server (npm audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cd mcp-server && npm ci
      - run: cd mcp-server && npm audit --audit-level=high

  backend:
    name: Backend (govulncheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          # Track go.mod's `go` directive automatically so this stays
          # in sync with the production Docker image's golang base.
          # Without go-version-file, we'd have to manually bump this
          # alongside every Go version change in the Dockerfile.
          go-version-file: 'backend/go.mod'
          # go.sum lives under backend/, not the repo root — point the
          # module cache at it so setup-go stops warning it can't find one.
          cache-dependency-path: 'backend/go.sum'
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      # Run in binary mode rather than source mode: govulncheck v1.4.0's
      # source-mode call-graph analysis segfaults (nil deref in
      # vulncheck.vulnFuncs) when analysing code built with the Go 1.26.x
      # toolchain that go.mod pins. Binary mode uses the compiled binary's
      # symbol table instead of source SSA, avoiding the crashing path while
      # still detecting reachable vulnerable symbols.
      - run: cd backend && go build -o mdnest .
      - run: cd backend && govulncheck -mode=binary ./mdnest

  shell:
    name: Shell scripts (shellcheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shellcheck
        # Severity: `error` only. The codebase currently has a handful
        # of `warning` and `info`-level findings (unused-var hints,
        # quote suggestions) that are worth addressing in follow-up
        # but aren't security-relevant. Bump severity here when those
        # are cleaned up.
        run: |
          shellcheck --version
          shellcheck -S error \
            setup.sh \
            mdnest-server \
            mdnest \
            git-sync/sync.sh \
            install-cli.sh \
            .githooks/pre-push
